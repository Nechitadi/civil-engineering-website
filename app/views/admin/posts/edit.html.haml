.container.admin-posts-container.pb-5
  .admin-posts-edit.text-center
    %h3.text-center Edit Post
    - if @post.featured_image.attached?
      = image_tag(url_for(@post.featured_image), class: 'featured-image')
    = render 'form'
